name: reusable-1

on:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout Code
        uses: actions/checkout@v4

      - name: 🛠️ Reusable 1
        shell: bash
        run: |
          echo "Running reusable 1"
          echo "$SUPER_SECRET" >> hello_world.txt
          cat hello_world.txt
        env:
          SUPER_SECRET: ${{ secrets.SUPER_SECRET }}