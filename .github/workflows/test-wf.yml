name: workflow-test

on:
  workflow_dispatch:


permissions:
  contents: read
  actions: write

jobs:
  job-uat-1:
    environment: 'uat'
    uses: ./.github/workflows/reusable-1.yml

  job-uat-2:
    environment: 'uat'
    uses: ./.github/workflows/reusable-2.yml